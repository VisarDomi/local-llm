# Harness Status

This directory is reserved for local agent/harness integration work.

## Current State

Harness setup is still a work in progress. There is no versioned harness code in
this repo yet.

What is currently documented elsewhere:

- `continue.md` — current session handoff and next steps
- `decisions.md` — benchmark conclusions, engine layout, and build commands

What is currently configured outside this repo:

- Pi CLI install/config
- Codex CLI install/config
- shell aliases and local machine setup

## Intended Use

When harness-specific scripts, configs, or reproducible test flows become stable
enough to keep, put them here instead of leaving them only in ad hoc shell state.
