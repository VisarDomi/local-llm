models/*.gguf

# Nested engine repos are managed separately from the parent workspace.
engine/dev/
engine/stable/

# Local Python tooling environment for GGUF conversion/build helpers.
engine/venv/

# Runtime state from the local server wrapper.
.llm-server.pid
.llm-server.log
.llm-server.state

# Local OCR experiment environment.
.venv-paddleocr/
