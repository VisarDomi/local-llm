# Notes

- Mixed case: printed profile box text plus handwritten-style pink caption/interjections.
- `source.png` is the normalized OCR input copy derived from the original `webp`.
- Useful for checking whether the engine drops stylized text while still reading clean body text.
